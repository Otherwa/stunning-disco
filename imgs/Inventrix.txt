function foureDigits(parameters: temp)
	multiply temp with 10 and assign to num
	initialize name to "Inventrix make it happen"
	find length of name and subtrac 100/10 then add to num
	convert num to string and assign to n 
	add 00 to n
	print n
	end function

function threeDigits()
	declare i and j int variable
	assign ('j' + 'a' + 'v' + 'a') to i
	initialize name to "@funzone"
	assign charAt index 1 of name to j
	print (i-j)
	//HINT: ASCII j=106,a=97,v=118,f=102
	end function

function lastDigits()
	 /* Q: If you take 4 years, how many days will be there altogether?
            *****Find answer and subtrac following output*****
       */
	if true
		print("501")
		end if
	end function

function main()
	call foureDigits(arguments:6)
	call threeDigits()
	call lastDigits()